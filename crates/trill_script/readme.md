Trill-script is a simple lisp-inspired language for defining dialog rules. 

A trill module (`.trl`) contains a sequence of definitions. Each definition is an S-expression beginning with a keyword. There are three types of definitions: `criterion`, `rule`, and `response`.

```

```
